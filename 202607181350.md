---
tags: [database, backup, debugging]
---

# RMAN Duplicate Log

Different to restore operation, the duplication operation usually starts with
`allocate auxiliary channel`.

The command `duplicate target database to 'target-database' PFILE=...` specifies
the [Parameter File](202607152311.md) used to start the auxiliary database.
